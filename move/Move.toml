[package]
name = "liber"
edition = "2024.beta"
# version is informational; bump on breaking changes to the registry object.
version = "0.1.0"

[dependencies]
Sui = { git = "https://github.com/MystenLabs/sui.git", subdir = "crates/sui-framework/packages/sui-framework", rev = "framework/testnet" }

[addresses]
liber = "0x0"
